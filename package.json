{
  "name": "glacier-route",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build": "npm run build --prefix apps/web",
    "install-web": "npm install --prefix apps/web"
  }
}
